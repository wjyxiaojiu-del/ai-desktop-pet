# 演示脚本（3 个场景）

## 场景 1：自然聊天 + 情感变化

**步骤**：
1. 启动程序：`python app/main.py`
2. 输入："我今天有点累"
3. 观察：
   - 宠物表情变为 😿（sad）
   - 宠物做摇头动画
   - LLM 用温暖语气安慰
   - 语音播报回复

**截图要点**：
- 聊天记录显示用户输入和宠物回复
- 情绪标签显示 sad
- 宠物做动画的瞬间

---

## 场景 2：视觉感知 + 主动打招呼

**步骤**：
1. 程序运行中，离开摄像头前
2. 等待 10 秒以上
3. 观察：宠物进入 😴 sleepy 状态
4. 回到摄像头前
5. 观察：
   - 宠物检测到人脸
   - 表情变为 😸（happy）
   - 主动跳跃打招呼
   - 气泡显示"[小九看到你啦！]"

**截图要点**：
- sleepy 状态截图
- 检测到人脸后的 happy 状态
- 跳跃动画瞬间

---

## 场景 3：智能家居控制

**前置**：HA 令牌已配置

**步骤**：
1. 输入："帮我打开客厅灯"
2. 观察：
   - LLM 在系统提示中判断需要调用工具
   - 回复中包含 ````tool_call` JSON
   - 程序自动执行 turn_on("light.living_room")
   - 最终回复："已打开客厅灯~"
   - 宠物变为 happy + 跳跃

**如果没有 HA**：
- 输入同样的话
- 观察 LLM 输出了 tool_call JSON
- 程序尝试调用但报错
- 仍然可以演示"意图理解 → 工具调用"的完整链路

**截图要点**：
- 用户输入"打开客厅灯"
- 宠物回复中包含工具调用意图
- 成功执行后的反馈

---

## 录屏建议

1. **总时长**：1-2 分钟
2. **分辨率**：1280x720 或更高
3. **不要录到 API Key**：确保 `.env` 文件不在画面内
4. **加上字幕**（可选）：用剪映等工具自动识别语音加字幕
5. **背景音乐**（可选）：轻快温馨的 BGM

## 简历 GIF/截图

从录屏中截取 3-5 个关键帧：
- 宠物正常状态（neutral）
- 宠物开心状态（happy + 跳跃）
- 宠物睡觉状态（sleepy）
- 聊天记录截图
- 语音按钮交互截图

导出为 GIF（用 ScreenToGif 等工具），放在 README 中展示。
